import { Card } from 'components/board/Card';
import PostView from 'components/post/Postview';
import { useParams } from 'react-router-dom';

function PostListItemPage() {
  return <PostView />;
}

export default PostListItemPage;
